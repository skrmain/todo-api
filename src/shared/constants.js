const TodoStatus = {
    created: 'created',
    done: 'done',
    archive: 'archive',
};

const ModelNames = {
    todo: 'todos',
    user: 'users',
};

module.exports = {
    TodoStatus,
    ModelNames,
};
